# CLAUDE.md — Phone

## Project Overview

**Phone** is a new project in early-stage development. The repository is hosted on GitHub at `Gribiche64/Phone`.

## Repository Structure

```
Phone/
├── README.md        # Project readme
└── CLAUDE.md        # This file — guidance for AI assistants
```

As the project grows, update this section to reflect the actual directory layout.

## Development Setup

### Prerequisites

_No dependencies have been configured yet._ Update this section when a build system, package manager, or language runtime is chosen.

### Getting Started

```bash
git clone <repo-url>
cd Phone
```

## Git Workflow

- **Default branch:** `main`
- Feature branches should follow the naming convention `<scope>/<description>`
- Write clear, concise commit messages describing the "why" not just the "what"
- Keep commits focused — one logical change per commit

## Build / Test / Lint Commands

_No build tooling is configured yet._ When added, document the key commands here:

```
# Example placeholders — replace when tooling is set up
# Build:  <command>
# Test:   <command>
# Lint:   <command>
# Format: <command>
```

## Code Conventions

- Keep code simple and readable; avoid premature abstractions
- Follow the idioms and style of whatever language/framework is adopted
- Do not introduce unnecessary dependencies
- Write tests alongside new functionality

## Key Guidelines for AI Assistants

1. **Read before writing** — always read existing files before proposing changes
2. **Minimal changes** — only modify what is directly needed; avoid drive-by refactors
3. **No guessing** — if context is missing, ask rather than assume
4. **Security first** — never introduce credentials, secrets, or known vulnerabilities
5. **Keep CLAUDE.md current** — update this file when the project structure, tooling, or conventions change
